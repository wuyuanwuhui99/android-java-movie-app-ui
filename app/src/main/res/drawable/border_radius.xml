<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!--背景颜色-->
    <solid android:color="#fff" />
    <!--分别设置左上，右上，左下，右下-->
    <corners
        android:bottomLeftRadius="@dimen/md_size"
        android:bottomRightRadius="@dimen/md_size"
        android:topLeftRadius="@dimen/md_size"
        android:topRightRadius="@dimen/md_size" />
</shape>